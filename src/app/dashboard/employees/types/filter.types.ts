// EmployeeSearchFilter now uses Zustand store directly
// This interface is kept for backward compatibility if needed
export interface EmployeeSearchFilterProps {
  placeholder?: string;
  className?: string;
}

