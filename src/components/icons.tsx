import { createIcon } from '@/components/ui/svg-icon';

// Create all your custom icons in one place
export const CartIcon = createIcon('/Icons/cart.svg', 'Shopping Cart');
export const ExcelIcon = createIcon('/images/Excel.png', 'Excel');

// Export the createIcon function for creating new icons on the fly
export { createIcon };
